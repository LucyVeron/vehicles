function Colors() {
  return (
    <div className="Colors">
      {/* <Label />
        {colors.map((color: any) => {
            return <ColorBox key={color} {...color} />;
        })}
        */}
    </div>
  );
}

export default Colors;
